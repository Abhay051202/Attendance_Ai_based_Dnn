# Face Recognition Attendance System - Requirements
# Install: pip install -r requirements.txt

# --- CORE LIBRARIES ---
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0

# --- FACE RECOGNITION ---
# Note: For InsightFace on Windows, you may need a specific wheel or "pip install insightface"
insightface>=0.7.3
onnxruntime-gpu>=1.16.0; sys_platform == 'win32' # Recommended for NVIDIA GPU
onnxruntime>=1.16.0; sys_platform != 'win32'     # CPU fallback

# --- TRACKING & UTILS ---
supervision>=0.16.0
requests>=2.31.0

# --- DATABASE ---
mysql-connector-python>=8.0.0

# --- GUI ---
# tkinter is built-in, no install needed

# --- EXTRAS ---
pyttsx3>=2.90        # Text-to-Speech
reportlab>=4.0.0     # PDF Generation
